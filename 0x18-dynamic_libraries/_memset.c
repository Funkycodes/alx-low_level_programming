#include "main.h"
/**
 * _memset - set n bytes of an address to a particular value
 * @s: memory address
 * @b: value to set to
 * @n: number of bytes to set
 * Return: int
 */
char *_memset(char *s, char b, unsigned int n)
{
}