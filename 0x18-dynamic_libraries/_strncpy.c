#include "main.h"
/**
 * _strncpy - copies a given number of characters to a string
 * @dest: string to concatenate to
 * @src: source of limited characters
 * @n: number of bytes to concatenate
 * Return: int
 */
char *_strncpy(char *dest, char *src, int n)
{
}