#include "main.h"
/**
 * _strchr - returns the address of the first occurence of a charcter
 * @s: srtring
 * @c: value to search for
 * Return: pointer to character found
 */
char *_strchr(char *s, char c)
{
}