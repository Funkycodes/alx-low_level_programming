#include "main.h"

/**
 * _abs - returns absolute value of given argument
 * @n: number to be checked
 *
 * Return: absolute value of number
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
